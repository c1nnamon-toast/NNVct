Non-visual changes:
!  : Fully connected layer doubled edges
   : Add bias support   
   : Add user input on the page   
   : Relative sizes of purples
   : Redo abstract layout nodes positioning 
!  : Add time load measurements to all pages
!  : Transfer files names to snake_case
!  : Cache the abstarct layout
!  : change w(i, j) to w(j, i)
!  : create diagram of programs flow
!  : backend separate folder for data
!  : test convertion of the "same" model in  tensorflow,  pytorch,  scikit-learn  to  onnx

Visual changes:
!! : Something is off with NN centering
!  : Fully connected layer doubled edges
   : Find a better way to highlight the hovered node and its adjacent edges
   : Relative sizes of purples
+- : Find the best way to place purples (currently automated) 


Suggestions:
   : Illustrate the "Flow" of the Network forward (and backward if present) 